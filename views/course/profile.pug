extends ../layout

block content
    h3= course.name
    a(href=`/course/edit/${course.id}`) Edit Course
    div
        p Instructor: #{course.instructor_name}
        p Department: #{course.department}
        p= course.description